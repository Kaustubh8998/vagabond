<div class="row m-0">
    <div class="col-sm-12 holder"> <!--href=""-->
        <div class="landing">
            <img class="" src="{{ url($slide->image) }}" alt="" width="100%">
            <!-- <div class="black">

                @if($slide->logo)
                <img class="" src="{{ url($slide->logo) }}" alt="" width="100">
                @endif

                @if($slide->text)
                <h3>{{ $slide->text }}</h3>
                @endif
                
                @if($slide->description)
                <p>{{ $slide->description }}</p>
                @endif
                
            </div> -->
        </div>
    </div>
</div>
